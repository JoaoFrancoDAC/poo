public interface ITransmiteDados {
    
    // Metodos em interface sao abstratos por padrão
    //Então não é preciso explicitar.

    void EnviaJson();
    String recebeMensagem(String protocolo);



    
}
